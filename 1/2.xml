<?xml version="1.0" encoding="UTF-8" ?>
<document>
<text>Внутри sqlite3 можно выполнять команды SQL или так называемые метакоманды (или dot-команды).
К метакомандам относятся несколько специальных команд для работы с SQLite. Они относятся только к утилите sqlite3, а не к SQL языку. В конце этих команд ; ставить не нужно.
</text>
<picture>1.png</picture>
<table cols='Название команды,Описание'>.open
  Открывает существующую, либо создает новую БД
  .show
  Показывает текущие настройки заданных параметров
  .databases
  Показывает название баз данных и файлов
  .quit
  Выход из sqlite3
  .tables
  Показывает текущие таблицы
  .schema
  Отражает структуру таблицы
  .header
  Отобразить или скрыть шапку таблицы
  .mode
  Выбор режима отображения данных таблицы
  .dump
  Сделать копию базы данных в текстовом формате
</table>
<text>В рамках даннго приложения, вы можете использовать команды: .open, .databases, .tables, .schema, .dump. Команды .header и .mode установлены по умолчанию для удобства чтения таблиц.</text>
</document>    
  